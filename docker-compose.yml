services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./artifacts:/app/artifacts
      - ./data:/app/data
    environment:
      - SENTINEL_MODEL_PATH=/app/artifacts/models/risk_model.joblib
